# Poll the mailbox for new newsletters. Install with:
#   sudo cp deploy/textcast-mail.* /etc/systemd/system/
#   sudo systemctl enable --now textcast-mail.timer
[Unit]
Description=Fetch newsletters into textcast

[Timer]
OnCalendar=*:0/30
Persistent=true

[Install]
WantedBy=timers.target
